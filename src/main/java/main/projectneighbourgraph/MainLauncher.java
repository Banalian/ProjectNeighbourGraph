package main.projectneighbourgraph;


/**
 * Class used to launch the program when outside the IDE.
 * Is needed because Java and JavaFX do not allow for direct laucnh of the program via the main application class.
 */
public class MainLauncher {
    public static void main(String[] args) {
        MainApplication.main(args);
    }
}